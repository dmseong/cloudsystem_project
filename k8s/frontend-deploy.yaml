# frontend-deploy.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: react-frontend
  namespace: moodtracker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: react-frontend
  template:
    metadata:
      labels:
        app: react-frontend
    spec:
      containers:
        - name: react-frontend
          image: kwonyujin04/cloud_frontend:12.04.tue
          imagePullPolicy: Never
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: react-frontend
  namespace: moodtracker
spec:
  selector:
    app: react-frontend
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
